# 📑 Документация API — Salon Project

Данный раздел описывает доступные эндпоинты сервера, структуру запросов и коды ответов. Все API-маршруты имеют базовый префикс `/api`.

---

## 👨‍🎨 1. Мастера (Masters)

### GET `/api/masters`
Получение списка мастеров (на данный момент возвращает статичный список).

* **Метод:** `GET`
* **Ответ (200 OK):**
    ```json
    [
      { "id": 1, "name": "Анна" },
      { "id": 2, "name": "Ирина" },
      { "id": 3, "name": "Елена" }
    ]
    ```

---

## 💅 2. Услуги (Services)

### GET `/api/services`
Получить список всех доступных услуг из базы данных.

### POST `/api/services`
Создать новую услугу.
* **Тело запроса (JSON):**
    ```json
    {
      "name": "Название услуги",
      "price": 1500
    }
    ```

### PATCH `/api/services/:id`
Обновить существующую услугу. Можно передать только одно поле (`name` или `price`).

### DELETE `/api/services/:id`
Удалить услугу из базы данных.
* **Ответ:** `204 No Content` при успехе.

---

## 📅 3. Бронирование и Заказы (Booking)

### POST `/api/booking`
Создание новой записи на стрижку/услугу. Перед созданием сервер проверяет:
1. Работает ли мастер в выбранный день недели.
2. Не занято ли выбранное время другим активным заказом.

* **Тело запроса (JSON):**
    ```json
    {
      "masterId": "1",
      "serviceId": "uuid-услуги",
      "date": "2023-10-25",
      "slot": "14:30",
      "name": "Клиент",
      "phone": "89001234567"
    }
    ```
* **Ошибки:**
    * `400`: Мастер не работает в этот день.
    * `409`: Это время уже забронировано.

### GET `/api/orders`
Получить список всех заказов (для панели администратора). Отсортировано по дате создания.

### PATCH `/api/orders/:id/status`
Обновить статус заказа.
* **Допустимые статусы:** `new`, `confirmed`, `cancelled`, `completed`.
* **Тело запроса:** `{ "status": "confirmed" }`

---

## ⏰ 4. Расписание (Schedules)

### GET `/api/schedules`
Получить расписание всех мастеров (рабочие дни и часы смены).

### PATCH `/api/schedules/:masterId`
Обновить или создать (upsert) расписание для мастера.
* **Тело запроса (JSON):**
    ```json
    {
      "work_days": ["Mon", "Tue", "Fri"],
      "start_of_shift": "10:00",
      "end_of_shift": "19:00"
    }
    ```

---

## 🛠 Техническая информация

### Обработка ошибок
Сервер возвращает стандартные HTTP-статусы:
* `200 / 201`: Успешная операция.
* `400`: Ошибка в данных (пропущены поля).
* `404`: Ресурс (услуга/заказ) не найден.
* `500`: Ошибка базы данных или сервера.

### Работа со статикой
Сервер автоматически отдает клиентскую сборку из папки `../build`. Если маршрут не совпадает ни с одним API, сервер вернет `index.html` 
